/**
 * API Routes for Mastra backend
 *
 * This file defines the HTTP endpoints that expose agents and workflows
 * to the frontend via REST API.
 *
 * For now, we'll use an empty array since Mastra auto-generates agent endpoints
 */

export const apiRoutes = [];
